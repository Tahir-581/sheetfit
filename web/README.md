# Web

Next.js UI for Sheetfit. Requires the Python engine on port 8765.

```bash
# from repo root, with engine venv already installed
npm run engine   # terminal 1
npm run web      # terminal 2
```

Set `NEXT_PUBLIC_SHEETFIT_API` if the API is not at `http://127.0.0.1:8765`.
